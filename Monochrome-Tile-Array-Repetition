float rectFill=0;

void setup(){
 size(500, 500); 
 background(255);
}
 
void draw(){
  frameRate(5);
  for(int i=0;i<width;i+=50){
    for(int j=0;j<height;j+=10){
      noStroke();
      fill(rectFill);
      rectFill = random(255);
      rect(i,j,50,10);
    }
  }
}
